[package]
name = "mod_metadata"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]
error = { path = "../error" }
hashing = { path = "../hashing" }
http_client = { path = "../http_client" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
