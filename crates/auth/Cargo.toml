[package]
name = "auth"
version = "0.1.0"
edition = "2021"

[dependencies]
error = { path = "../error" }
http_client = { path = "../http_client" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_urlencoded = "0.7"
