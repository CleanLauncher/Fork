name: E2E Quilt
on: [push, pull_request]
jobs:
  test-quilt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cargo test test_quilt --manifest-path crates/core/Cargo.toml
