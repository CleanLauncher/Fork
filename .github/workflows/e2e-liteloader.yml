name: E2E LiteLoader
on: [push, pull_request]
jobs:
  test-liteloader:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cargo test test_liteloader --manifest-path crates/core/Cargo.toml
